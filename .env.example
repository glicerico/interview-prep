# API Keys
# These are only necessary if they are not setup in your .bashrc environment variables
# OPENAI_API_KEY=your_openai_api_key
# QWELLO_API_KEY=your_qwello_api_key

# Redis Configuration
# For Docker containers using host network, use localhost:
REDIS_HOST=127.0.0.1
# For Docker containers in a specific network, use the container name:
# REDIS_HOST=hrsdk-redis-1
REDIS_PORT=6379
REDIS_DB=0